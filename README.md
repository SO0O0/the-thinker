# The thinker

## What is this?

## How to use

- ### Build

    ```bash
    docker-compose up -d
    ```

- ### Run

    ```bash
    docker-compose exec app bash
    cd src && npm install && npm run dev
    ```

    #### Access to http://localhost:3000
